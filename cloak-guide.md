# راهنمای کامل cloak

## این ابزار چیه و چی نیست

`cloak` یه پراکسی محلیه که TLS handshake واقعیت رو پشت یه handshake جعلی با SNI مجاز پنهان می‌کنه (نه فرگمنت خام). **این «اسپوف واقعیِ هم‌فلو» نیست** — اون یکی همیشه به روت نیاز داره، چون فقط از طریق raw socket (`CAP_NET_RAW`) ممکنه. `cloak` یه decoy جدا با TTL پایین می‌فرسته که قبل از رسیدن به سرور واقعی می‌میره، و چند تا ترفند دیگه (پورت مشترک، چند TTL، race کردن سرورها، آمار تطبیقی) رو روش سوار کرده تا نزدیک‌ترین چیز ممکن به اسپوف واقعی باشه — همه بدون روت.

---

## ۱. نصب و کامپایل

روی Termux:

```bash
pkg update && pkg install clang
```

بعد فایل `cloak.c` رو کپی کن و کامپایلش کن:

```bash
gcc -O2 -Wall -o cloak cloak.c -lpthread
```

اگه یه هشدار درباره‌ی `strncpy` دیدی، بی‌ضرره — کد یه null-terminate دستی بعدش داره.

---

## ۲. اولین اجرا

```bash
./cloak
```

چون هنوز `cloak.conf` نداری، خودش می‌سازدش و خارج میشه:

```
no config file found at 'cloak.conf' -- writing a starter one for you.
edit it, then run this program again.
```

فایل رو ویرایش کن (`nano cloak.conf`)، بعد دوباره `./cloak` بزن — از این به بعد فقط همین دستور کافیه.

اگه چند تا کانفیگ جدا برای سرورهای مختلف می‌خوای:

```bash
./cloak server1.conf
./cloak server2.conf
```

---

## ۳. مرجع کامل تنظیمات کانفیگ

### `listen_port`
پورت محلی که کلاینت VPN/پراکسیت (v2rayNG، Xray، NapsternetV، …) باید بهش وصل بشه.

```ini
listen_port = 40443
```

توی v2rayNG: آدرس outbound رو `127.0.0.1:40443` بذار.

---

### `connect_list`
یک یا چند سرور مقصد واقعی. `cloak` همه‌شون رو **هم‌زمان** probe می‌کنه و هر کدوم زودتر جواب داد رو استفاده می‌کنه (race کردن).

```ini
# فقط IPv4
connect_list = 104.18.38.202:443

# چند سرور (fallback خودکار)
connect_list = 104.18.38.202:443, 104.18.39.100:443

# IPv6 (حتماً با کروشه، چون خودِ آدرس پر از : هست)
connect_list = [2606:4700::1]:443

# ترکیب IPv4 و IPv6 با هم
connect_list = 104.18.38.202:443, [2606:4700::1]:443
```

⚠️ **فقط IP خام قبول میشه، نه دامنه.** اگه دامنه بذاری، رد میشه و هشدار می‌ده — چون resolve کردنش یه DNS query رمزنگاری‌نشده می‌فرسته که خودش SNI واقعی رو لو می‌ده.

**چطور IP تمیز پیدا کنم؟** یه سرویس پشت Cloudflare (یا هر CDN دیگه) لازم داری. رنج آی‌پی‌های Cloudflare رو می‌تونی از `https://www.cloudflare.com/ips/` بگیری، یا از اسکنرهای عمومی SNI (که قبلاً درباره‌شون صحبت کردیم) IPهای تمیز فعلی رو پیدا کنی — چون این لیست‌ها مدام عوض میشن.

---

### `sni_list`
دامنه‌های مجاز/بی‌خطر که به‌عنوان SNI جعلی استفاده میشن. هر اتصال جدید یکی رو رندوم انتخاب می‌کنه.

```ini
sni_list = www.hcaptcha.com, www.speedtest.net, www.bing.com
```

**نکته:** دامنه‌هایی انتخاب کن که مطمئنی فیلتر نیستن و روی همون سرور CDN که توی `connect_list` گذاشتی جواب میدن.

---

### `ttl_list`
لیست TTLهایی که برای decoy امتحان میشن، از کم به زیاد. `cloak` برای هر مقدار یه decoy جدا می‌فرسته.

```ini
ttl_list = 4, 5, 6, 8
```

هرچی این لیست متنوع‌تر باشه، شانس اینکه دقیقاً همون hopـی که DPI توش نشسته رو بزنی بیشتره — ولی هر مقدار اضافه یعنی یه پکت اضافه در ابتدای هر اتصال (تأخیر کمی بیشتر).

---

### `jitter_min_ms` / `jitter_max_ms`
تأخیر تصادفی (میلی‌ثانیه) بین فرستادن decoy و فرستادن ClientHello واقعی.

```ini
jitter_min_ms = 20
jitter_max_ms = 80
```

عمداً رندومه تا الگوی زمان‌بندیِ ثابت نداشته باشیم.

---

### `calibrate`
`true` یا `false`. اگه فعال باشه، به‌جای استفاده‌ی مستقیم از `ttl_list`، هر اتصال یه traceroute بی‌نوا (فقط با `connect()`، بدون raw socket) اجرا می‌کنه تا فاصله‌ی سرور رو حدس بزنه.

```ini
calibrate = false
```

**پیشنهاد:** بذارش `false` و به‌جاش `adaptive` رو روشن نگه دار — نتیجه‌ی واقعی‌تری میده. `calibrate` روی شبکه‌ی موبایل که ICMP رو گاهی می‌بلعه، می‌تونه نتیجه‌ی نامعتبر بده.

---

### `fragment`
`true` یا `false`. اگه فعال بشه، ClientHelloِ واقعی هم (علاوه بر decoy) به تیکه‌های کوچیک تقسیم میشه.

```ini
fragment = false
```

برای DPIهایی که فقط pattern-match می‌کنن (نه flow-tracking) مفیده. اگه `cloak` به‌تنهایی جواب نداد، این رو فعال کن.

---

### `adaptive`
`true` یا `false`. اگه فعال باشه، `cloak` نتیجه‌ی هر اتصال (موفق/ناموفق، بر اساس اینکه جواب واقعیِ TLS از سرور گرفته یا نه) رو توی `cloak.stats` ذخیره می‌کنه و دفعه‌ی بعد اول بهترین TTL/سرورها رو امتحان می‌کنه.

```ini
adaptive = true
```

**پیشنهاد اکید: همیشه روشن نگه‌دار.** تنها راهیه که `cloak` واقعاً با شرایط شبکه‌ی خودت تطبیق پیدا می‌کنه.

---

### `verbose`
`true` یا `false`. لاگ‌های `[decoy]`/`[real]`/`[stats]` رو نشون میده یا نه.

```ini
verbose = true
```

برای دیباگ اولیه `true` بذار، بعد از اینکه مطمئن شدی کار می‌کنه، می‌تونی `false` کنی تا ترمینال شلوغ نشه.

---

## ۴. سناریوهای رایج (چطور انواع اتصال رو بزنم)

### سناریوی ۱: یه سرور ساده، بدون کالیبراسیون
```ini
listen_port = 40443
connect_list = 104.18.38.202:443
sni_list = www.hcaptcha.com, www.bing.com
ttl_list = 4, 5, 6
jitter_min_ms = 20
jitter_max_ms = 80
calibrate = false
fragment = false
adaptive = true
verbose = true
```
برای شروع و تست اولیه مناسبه.

### سناریوی ۲: چند سرور fallback (اگه یکی بلاک شد)
```ini
connect_list = 104.18.38.202:443, 104.18.39.100:443, 172.67.10.10:443
```
`cloak` خودش هر بار سریع‌ترینِ در دسترس رو انتخاب می‌کنه.

### سناریوی ۳: IPv6 (اگه اپراتورت IPv6 میده)
```ini
connect_list = [2606:4700::1]:443, 104.18.38.202:443
```
هم IPv4 هم IPv6 با هم، `cloak` بین‌شون race می‌کنه.

### سناریوی ۴: علیه DPI سرسخت‌تر (هم decoy هم فرگمنت)
```ini
ttl_list = 3, 4, 5, 6, 7, 8
fragment = true
```

### سناریوی ۵: دیباگ یه اتصال جدید
```ini
verbose = true
adaptive = true
```
لاگ‌ها رو نگاه کن، ببین کدوم TTL/سرور موفق میشه، بعد از چند اتصال `cloak.stats` رو چک کن.

---

## ۵. وصل کردن به v2rayNG / Xray

1. `cloak` رو اجرا کن، مطمئن شو پیام `listening on 127.0.0.1:PORT` رو دیدی.
2. توی v2rayNG یه کانفیگ VLESS/Trojan/VMess عادی بساز (همون که از سرورت گرفتی).
3. توی تنظیمات outbound همون کانفیگ، آدرس سرور رو به `127.0.0.1` و پورت رو به همون `listen_port` که توی `cloak.conf` گذاشتی تغییر بده.
4. باقی تنظیمات (UUID، path، …) دست‌نخورده می‌مونه، چون `cloak` فقط لایه‌ی TLS/SNI رو دستکاری می‌کنه، نه پروتکل بالادستی.

---

## ۶. خوندن `cloak.stats`

فایل به این شکله:
```
TTL 4 12 3
TTL 5 8 7
SRV 104.18.38.202 443 15 5
```
یعنی: `TTL 4` تا الان ۱۲ بار موفق و ۳ بار ناموفق بوده. `SRV` همون برای هر سرور. هرچی عدد موفقیت بیشتر باشه نسبت به شکست، اون TTL/سرور اولویت بیشتری توی اتصال‌های بعدی می‌گیره. نیازی نیست دستی ویرایشش کنی — فقط برای دیدِ کلی مفیده.

---

## ۷. خاموش کردن

```
Ctrl+C
```
تمیز خارج میشه و پیام «shutting down» و «stats saved» رو نشون میده. یادت باشه: اتصال‌های در حال کار همون لحظه قطع میشن (این یه پراکسی محلیه، نه یه سرویس که باید داده رو تا ته نگه داره).

---

## ۸. مشکلات رایج

| علامت | احتمالاً چیه |
|---|---|
| `no server in connect_list answered in time` | همه‌ی IPهای `connect_list` بلاک/غیرقابل‌دسترسی‌ان — IPهای تازه پیدا کن |
| اتصال برقرار میشه ولی سرعت خیلی پایینه | `ttl_list` رو کوتاه‌تر کن (پکت اضافه = تأخیر اضافه)، یا `fragment` رو خاموش کن اگه لازم نیست |
| بعد از مدتی هیچ TTLای جواب نمیده | یعنی DPI رفتارشو عوض کرده؛ `sni_list` و `ttl_list` رو تازه کن |
| `[config] WARNING: ... is not a raw IPv4/IPv6` | توی `connect_list` دامنه گذاشتی به‌جای IP — طبق طراحی رد میشه |

---

## ۹. محدودیت صادقانه (یادت باشه)

`cloak` سقفِ واقعیِ بدون‌روت رو به حداکثر رسونده، ولی هیچ‌وقت معادل کامل اسپوفِ seq-based (که فقط با روت ممکنه) نیست. اگه یه روز روت کردی، همون کد C که قبلاً باهم نوشتیم (`real_sni_spoof.c`) رو داری که نسخه‌ی raw-socket واقعیه.
